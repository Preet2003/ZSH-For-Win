# WinZSH runtime module template (reference only).
# Phase 2+ modules are fully generated by winzsh-runtime-gen.
Set-StrictMode -Version Latest

function Get-WinZshInfo {
    [CmdletBinding()]
    param()
    [pscustomobject]@{
        Name    = 'WinZSH'
        Phase   = 'phase-2'
        Message = 'WinZSH runtime loaded (Phase 2 UX).'
    }
}

Export-ModuleMember -Function Get-WinZshInfo
